.user_details
  .row
    .span1

    .span1
      = "#{current_user.first_name} #{current_user.last_name}"
    .span2
      = "#{User.human_attribute_name("phone_number")}: #{current_user.phone_number}"
    .span2
      = "#{User.human_attribute_name("email")}: #{current_user.email}"
    .offset6
