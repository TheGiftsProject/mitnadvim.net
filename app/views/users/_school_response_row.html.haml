.response_row.row
  != render partial: "components/request/request_details", locals: {req: response.request}
  .span4
    .actions
      .info
        %strong= response.request.category.name
      .action
        %a.btn{href: "#school_response_modal_#{response.id}", data: {toggle: "modal"}}= t("components.response_row.actions.watch")

  != render partial: "modals/volunteer/school_response_modal", locals: {response: response, id: "school_response_modal_#{response.id}"}