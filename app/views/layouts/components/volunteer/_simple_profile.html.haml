.user_details
  .row
    .span1

    .span1
      = "#{@user.first_name} #{@user.last_name}"
    .span2
      = "#{t("users.profile.phone_no")}: #{@user.phone_number}"
    .span2
      = "#{t("users.profile.email")}: #{@user.email}"
    .offset4
    .span2
      עפרטיפרטי פרופיל
